pub struct PacketHandler {
    
}

impl PacketHandler {
    pub fn new() {

    }

    /// Turns a packet into an array of bytes
    pub fn serialize() -> Vec<u8> {
        let some_vec: Vec<u8> = vec![1, 2, 3, 4];
        some_vec.
    }

    /// Turns a series of bytes into a packet
    pub fn deserialize() {

    }
}